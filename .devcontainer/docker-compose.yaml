version: "3.4"
services:
   taskrc-kit:
      build:
         context: .
         args:
             VARIANT: bullseye
      volumes:
        - ${HOME}:/host_home:ro
        - ${HOME}/.ssh:/home/vscode/.ssh:ro
        - ..:/workspace
      command:  sleep infinity
